<Window x:Class="Streamall.Views.AdministratorHome"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Streamall.Views"
        xmlns:contentUserControl="clr-namespace:Streamall.Views.UserControls"
        xmlns:viewModelContent="clr-namespace:Streamall.ViewModels.Contents"
        mc:Ignorable="d"
        WindowStyle="None"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        Background="{StaticResource BackgroundColorLoginBrush}"
        Title="AdministratorHome" Height="500" Width="900">
    <Window.Resources>
        <DataTemplate DataType="{x:Type viewModelContent:ContentManagementViewModel}">
            <contentUserControl:ContentManagement/>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <!-- Sidebar -->
            <ColumnDefinition Width="240"/>
            <!-- Área Principal -->
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- SIDEBAR -->
        <Border Grid.Column="0" Background="#141724" BorderBrush="#1F2436" BorderThickness="0,0,1,0">
            <Grid Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Logo -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10,10,10,30">
                    <TextBlock Style="{StaticResource StyleTextBlockLogoLogin}" Text="STREAMALL" FontSize="30"/>
                </StackPanel>

                <!-- Menu de Navegação -->
                <StackPanel Grid.Row="1" VerticalAlignment="Center">
                    <Button Content="Catálogo de Filmes" Style="{StaticResource MenuButtonStyle}" Foreground="#FFFFFF" Margin="0,0,0,5" Command="{Binding ShowContentsManagmentCommand}"/>
                    <Button Content="Cadastrar Filme" Style="{StaticResource MenuButtonStyle}" Margin="0,0,0,5"/>
                    <Button Content="Gerenciar Clientes" Style="{StaticResource MenuButtonStyle}" Margin="0,0,0,5"/>
                    <Button Content="Estatísticas" Style="{StaticResource MenuButtonStyle}" Margin="0,0,0,5"/>
                </StackPanel>

                <!-- Perfil do Usuário no Rodapé -->
                <Border Grid.Row="2" Background="#1E2235" CornerRadius="10" Padding="10">
                    <StackPanel Orientation="Horizontal">
                        <Border Width="36" Height="36" CornerRadius="18" Background="{StaticResource ButtonGradientLogin}">
                            <TextBlock Text="{Binding NameInitials}" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Margin="10,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="{Binding FullNameAdm}" Foreground="White" FontWeight="SemiBold" FontSize="13"/>
                            <TextBlock Text="Administrador" Foreground="#8A8F9E" FontSize="11"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- ÁREA PRINCIPAL COM BARRA DE TÍTULO SUPERIOR -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/>
                <!-- Barra de Controle -->
                <RowDefinition Height="*"/>
                <!-- Conteúdo -->
            </Grid.RowDefinitions>

            <!-- BARRA SUPERIOR (BOTOES DE CONTROLE DA JANELA) -->
            <Border Grid.Row="0" Background="Transparent" HorizontalAlignment="Stretch">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
                    <!-- Botão Minimizar -->
                    <Button x:Name="BtnMinimize" Style="{StaticResource TitleBarButtonStyle}" Click="BtnMinimize_Click" ToolTip="Minimizar">
                        <Path Data="M19,13H5V11H19V13Z" Fill="#8A8F9E" Width="12" Height="12" Stretch="Uniform"/>
                    </Button>

                    <!-- Botão Maximizar / Restaurar -->
                    <Button x:Name="BtnMaximize" Style="{StaticResource TitleBarButtonStyle}" Click="BtnMaximize_Click" ToolTip="Maximizar">
                        <Path Data="M4,4H20V20H4V4M6,8V18H18V8H6Z" Fill="#8A8F9E" Width="12" Height="12" Stretch="Uniform"/>
                    </Button>

                    <!-- Botão Fechar -->
                    <Button x:Name="BtnClose" Style="{StaticResource CloseButtonStyle}" Click="BtnClose_Click" ToolTip="Fechar">
                        <Path Data="M19,6.41L17.59,5L12,10.59L6.41,5L5,6.41L10.59,12L5,17.59L6.41,19L12,13.41L17.59,19L19,17.59L13.41,12L19,6.41Z" Fill="#8A8F9E" Width="12" Height="12" Stretch="Uniform"/>
                    </Button>
                </StackPanel>
            </Border>

            <!-- CONTEÚDO PRINCIPAL -->
            <ContentControl Content="{Binding CurrentViewModel}" Grid.Row="1"/>
        </Grid>
    </Grid>
</Window>