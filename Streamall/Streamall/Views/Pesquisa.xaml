<Page x:Class="Streamall.Views.Pesquisa"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Streamall.Views"
      xmlns:userControls="clr-namespace:Streamall.Views.UserControls"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Pesquisa">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0"
             Width="500"
             Margin="10"
             HorizontalAlignment="Left"
             Style="{StaticResource StyleTextBoxLogin}"
             Background="{StaticResource LogoPurpleLoginBrush}"
             Foreground="{StaticResource SurfaceColorLoginBrush}"
             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>

        <ScrollViewer Grid.Row="1"
                  VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Results}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="10"
                            Padding="10"
                            Background="{StaticResource BackgroundColorLoginBrush}"
                            Cursor="Hand"
                            MouseLeftButtonUp="SearchResult_Click">
                            <StackPanel Orientation="Horizontal">
                                <Image Source="{Binding PathCover}"
                                   Width="80"
                                   Height="115"
                                   Stretch="UniformToFill"
                                   Margin="0,0,12,0"/>

                                <TextBlock Text="{Binding Name}"
                                       Foreground="White"
                                       FontSize="18"
                                       VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>
