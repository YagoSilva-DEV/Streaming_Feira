<UserControl x:Class="Streamall.Views.UserControls.ClientManagement"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="900"
             MinWidth="620"
             Background="Transparent">

    <UserControl.Resources>
        <!-- Paleta de Cores e Pincéis -->
        <SolidColorBrush x:Key="BgCard" Color="#131622"/>
        <SolidColorBrush x:Key="BgHover" Color="#1C2132"/>
        <SolidColorBrush x:Key="BgSelected" Color="#252A3E"/>
        <SolidColorBrush x:Key="BorderSubtle" Color="#23283B"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#8C93A8"/>
        <SolidColorBrush x:Key="AccentPurple" Color="#8B5CF6"/>
        <SolidColorBrush x:Key="StatusActiveBg" Color="#102E26"/>
        <SolidColorBrush x:Key="StatusActiveFg" Color="#10B981"/>
        <SolidColorBrush x:Key="StatusInactiveBg" Color="#282026"/>
        <SolidColorBrush x:Key="StatusInactiveFg" Color="#EF4444"/>

        <!-- Estilo dos Itens da Lista (ListViewItem) -->
        <Style x:Key="ModernListViewItem" TargetType="ListViewItem">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="RowBorder" 
                                Background="{StaticResource BgCard}" 
                                BorderBrush="{StaticResource BorderSubtle}" 
                                BorderThickness="1" 
                                CornerRadius="12" 
                                Padding="14,10">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="RowBorder" Property="Background" Value="{StaticResource BgHover}"/>
                                <Setter TargetName="RowBorder" Property="BorderBrush" Value="#333A52"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="RowBorder" Property="Background" Value="{StaticResource BgSelected}"/>
                                <Setter TargetName="RowBorder" Property="BorderBrush" Value="{StaticResource AccentPurple}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Estilo Customizado da Caixa de Busca -->
        <Style x:Key="ModernSearchBox" TargetType="TextBox">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Background" Value="{StaticResource BgCard}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderSubtle}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="38,10,14,10"/>
            <Setter Property="CaretBrush" Value="{StaticResource AccentPurple}"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <Border x:Name="SearchBorder" 
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="10" 
                                    SnapsToDevicePixels="True">
                                <Grid>
                                    <!-- Ícone de Lupa em SVG -->
                                    <Path Data="M15.5 14h-.79l-.28-.27C15.41 12.59 16 11.11 16 9.5 16 5.91 13.09 3 9.5 3S3 5.91 3 9.5 5.91 16 9.5 16c1.61 0 3.09-.59 4.23-1.57l.27.28v.79l5 4.99L20.49 19l-4.99-5zm-6 0C7.01 14 5 11.99 5 9.5S7.01 5 9.5 5 14 7.01 14 9.5 11.99 14 9.5 14z" 
                                          Fill="{StaticResource TextSecondary}" 
                                          Width="16" Height="16" 
                                          HorizontalAlignment="Left" 
                                          Margin="14,0,0,0" 
                                          Stretch="Uniform"/>

                                    <!-- Placeholder (Texto quando vazio) -->
                                    <TextBlock x:Name="PlaceholderText" 
                                               Text="Buscar por nome ou usuário..." 
                                               Foreground="{StaticResource TextSecondary}" 
                                               FontSize="13" 
                                               Margin="38,0,0,0" 
                                               VerticalAlignment="Center" 
                                               IsHitTestVisible="False" 
                                               Visibility="Collapsed"/>

                                    <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                                </Grid>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="PlaceholderText" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="SearchBorder" Property="BorderBrush" Value="{StaticResource AccentPurple}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 1. Cabeçalho de Título -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <StackPanel HorizontalAlignment="Center">
                <TextBlock Text="Gerenciamento de Clientes" 
                           Style="{StaticResource StyleTextBlockTopAdmHome}"/>
                <TextBlock Text="Gerencie as contas e o status de acesso dos clientes" 
                           Style="{StaticResource StyleTextBlockDescriptionTopAdmHome}"/>
            </StackPanel>
        </Grid>

        <!-- 2. Campo de Busca -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <TextBox Style="{StaticResource ModernSearchBox}"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     ToolTip="Digite o nome ou usuário para filtrar"/>
        </Grid>

        <!-- 3. Cabeçalho da Tabela -->
        <Grid Grid.Row="2" Margin="16,0,16,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="48"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1.5*"/>
                <ColumnDefinition Width="110"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="1" Text="NOME COMPLETO" Foreground="{StaticResource TextSecondary}" FontSize="11" FontWeight="Bold"/>
            <TextBlock Grid.Column="2" Text="NOME DE USUÁRIO" Foreground="{StaticResource TextSecondary}" FontSize="11" FontWeight="Bold"/>
            <TextBlock Grid.Column="3" Text="STATUS" Foreground="{StaticResource TextSecondary}" FontSize="11" FontWeight="Bold" HorizontalAlignment="Center"/>
        </Grid>

        <!-- 4. Lista de Usuários -->
        <ListView Grid.Row="3" 
                  ItemsSource="{Binding Clients}"
                  ItemContainerStyle="{StaticResource ModernListViewItem}"
                  Background="Transparent" 
                  BorderThickness="0" 
                  Padding="0"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid MinHeight="44" VerticalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="48"/>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="1.5*"/>
                            <ColumnDefinition Width="110"/>
                        </Grid.ColumnDefinitions>

                        <!-- Avatar do Usuário (Mostra a primeira letra) -->
                        <Border Width="36" Height="36" CornerRadius="18" 
                                Background="#2A2440" 
                                HorizontalAlignment="Left" 
                                VerticalAlignment="Center">
                            <TextBlock Text="{Binding FullName[0]}" 
                                       Foreground="{StaticResource AccentPurple}" 
                                       FontSize="14" 
                                       FontWeight="Bold" 
                                       HorizontalAlignment="Center" 
                                       VerticalAlignment="Center"/>
                        </Border>

                        <!-- Nome Completo -->
                        <TextBlock Grid.Column="1" 
                                   Text="{Binding FullName}" 
                                   Foreground="{StaticResource TextPrimary}"
                                   FontSize="13" 
                                   FontWeight="SemiBold" 
                                   VerticalAlignment="Center" 
                                   TextTrimming="CharacterEllipsis"/>

                        <!-- Nome de Usuário -->
                        <TextBlock Grid.Column="2" 
                                   Foreground="{StaticResource TextSecondary}" 
                                   FontSize="12"
                                   VerticalAlignment="Center" 
                                   TextTrimming="CharacterEllipsis">
                            <Run Text="@"/><Run Text="{Binding UserName}"/>
                        </TextBlock>

                        <TextBlock Grid.Column="3" 
                           Text="{Binding StatusUser}" 
                           Foreground="{StaticResource TextSecondary}" 
                           FontSize="12" 
                           FontWeight="SemiBold" 
                           HorizontalAlignment="Center" 
                           VerticalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>